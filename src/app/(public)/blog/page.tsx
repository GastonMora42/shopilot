import UserGuideAndBlog from "@/components/blog/user-guide-and-blog";

export default function Blog() {
  return (
    <div className="flex flex-col min-h-screen bg-white">
      {/* Header */}
      {/* ... (keep the existing header code) */}

      {/* Hero Section */}
      {/* ... (keep the existing hero section code) */}

      {/* Events Section */}
      {/* ... (keep the existing events section code) */}

      {/* User Guide and Blog Section */}
      <UserGuideAndBlog />

      {/* ... (any other existing sections) */}
    </div>
  )
}
